import styles from './NavBar.module.css';

const NavBar = () => {
    return(
        <div className={styles.container}>
            &lt;-- nav bar goes here --&gt;
        </div>
    );
}

export default NavBar;